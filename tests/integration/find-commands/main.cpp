#include <iostream>
#include <cmath>

int main() {
    // Use the math library to verify it linked correctly
    double result = sqrt(16.0);
    std::cout << "sqrt(16) = " << result << std::endl;
    return 0;
}
